---
- name: Install base system packages
  apt:
    name: 
      - curl
      - git 
      - htop
      - ufw
      - nano
      - python3
      - python3-pip
      - acl
    state: present
    update_cache: yes 

  