---
- name: Install base system packages
  apt:
    name: 
      - curl
      - git 
      - htop
      - ufw
      - nano
      - python3
      - python3-pip
    state: present
    update_cache: yes 

  