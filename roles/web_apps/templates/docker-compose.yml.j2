services:

{% for web_app_name, app_config in web_services.items() %}
  {{ web_app_name }}:
    image: {{ app_config.image }}
    ports:
      - "127.0.0.1:{{ item.value.host_port }}:{{ item.value.container_port }}"
{% endfor %}
